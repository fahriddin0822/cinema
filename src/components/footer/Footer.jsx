import React from "react";
import logo from "@/assets/images/logo__footer.svg"

const Footer = () => {
  return <footer>
    <div>
        <img src={logo} alt="" />
    </div>
  </footer>;
};

export default Footer;
